<?php
    /*$dirs = [
        '', // app
        '/shared',

        /// modules
        '/modules/core',
        '/modules/home',
        '/modules/mobidul'
    ];

    foreach ( $dirs as $dx => $path )

        foreach ( glob('public/app' . $path . '/*.js') as $tx => $src )

            echo htmlentities('<script src="' . substr($src, 7, strlen($src)) . '"></script>') . '<br>';*/
